---
name: ci

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: read
  pull-requests: read

jobs:
  ci:
    uses: mpa-forge/.github/.github/workflows/reusable-ci-frontend.yml@4454175b41c288c2fe2052d84889336f3c3591b1 # yamllint disable-line rule:line-length
    secrets:
      GH_PACKAGES_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    with:
      working-directory: "."
      node-version: "24.13.1"
      bun-version: "1.3.11"
      test-command: "bun run test"
      sonar-organization-key: "mpa-forge"
      sonar-project-key: "mpa-forge_platform-frontend-observability"
